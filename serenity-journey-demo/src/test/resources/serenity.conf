webdriver.driver = chrome

serenity {
  compress.filenames = false
  project.name="Demo Project using Serenity and Cucumber"
//  take.screenshots = for_failures
  take.screenshots.for.performables = for_failures
  take.screenshots.for.interactions = for_failures
  take.screenshots.for.questions = for_failures
  take.screenshots.for.tasks = for_failures
  restart.browser.for.each = NEVER
}